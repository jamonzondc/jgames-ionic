import { GetColorByStatePipe } from './get-color-by-state.pipe';

describe('GetColorByStatePipe', () => {
  it('create an instance', () => {
    const pipe = new GetColorByStatePipe();
    expect(pipe).toBeTruthy();
  });
});
